============
Installation
============

At the command line::

    $ easy_install annotationrdf

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv annotationrdf
    $ pip install annotationrdf